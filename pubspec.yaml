
name: racebox_client
description: A Dart library for connecting to and communicating with RaceBox devices.
version: 0.0.1
publish_to: 'none' 

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter_blue_plus: ^1.31.5
  collection: ^1.17.0

